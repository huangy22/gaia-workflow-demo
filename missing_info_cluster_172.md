# cluster_172 信息缺失汇总

## 写给 workfdiv 总结的同学

本文档列出了当前 workflow JSON 中 **缺失但代码实现需要** 的信息，按优先级排列。

---

## 1. 关键物理参数（最高优先级）

### 1.1 耦合常数 λ

当前从 BCS 公式 $k_B T_c = 1.136\,E_0\,e^{-1/\lambda}$ 反推，但论文原文使用的精确值未知。

| 参数 | 当前状态 | 需要 |
|------|---------|------|
| $\lambda_A$ (A 相) | 设为 0.42（猜测） | 论文中报告或可反推的精确值 |
| $\lambda_B$ (B 相) | 设为 0.48（猜测） | 同上 |
| $\lambda$ 是否温度依赖 | 未考虑 | 明确是否常数 |

### 1.2 能量截断 $E_0$

| 参数 | 当前状态 | 需要 |
|------|---------|------|
| $E_0$ (meV) | 从 $T_c$ 和 $\lambda$ 反推 | 论文中使用的具体值或能标 |
| $E_0$ 是否对 A/B 相不同 | 按不同 $\lambda$ 分别计算 | 明确 |
| $E_0$ 的物理来源 | 未说明 | Debye 频率？Matsubara 截断？ |

### 1.3 角向形式因子 $f(\hat{k})$

这是 workflow 里最关键的缺失——**决定了能隙各向异性和节点结构**。

| 信息 | 当前状态 | 需要 |
|------|---------|------|
| $f(\hat{k})$ 的函数形式 | 未知 | 解析表达式（如 $f(\theta,\phi) = ...$） |
| A 相的具体 $f$ | 未知 | 可能是 $k_x^2 - k_y^2$ (d-wave) 等 |
| B 相的具体 $f$ | 未知 | 同上 |
| 费米面平均 $\langle f^2 \rangle_{FS}$ | 设为 1.0 | 积分数值 |
| 费米面几何 | 未描述 | PrOs₄Sb₁₂ 费米面形状或简化假设 |

### 1.4 能隙对称性分类

| 信息 | 当前状态 | 需要 |
|------|---------|------|
| A 相节点类型 | 推断为点节点 | 明确（点节点/线节点/全能隙） |
| B 相节点类型 | 同上 | 同上 |
| 混合态 $d_{x^2-y^2}+id_{xy}$ | 结论中提及 | 哪个相？何时出现？ |

---

## 2. 验证目标（用于校准代码）

### 2.1 已知道但来源不明的数值

| 可观测值 | 报告值 | 来源确认 |
|----------|--------|---------|
| $\Delta C/C$ (A 相) | ≃ 0.93 | 确认来自 ref6 的哪个表格/图 |
| $\Delta C/C$ (B 相) | ≃ 1.20 | 同上 |
| $T_c$ | ≃ 1.85 K | PrOs₄Sb₁₂ 已知值，确认论文中使用的精确值 |

### 2.2 需要的额外验证数据点

如果论文中有以下数据，请提供数值：

- $\Delta(0)$ / $k_B T_c$ 比值
- $\gamma_{ST}$ (正常态 Sommerfeld 系数)
- $C_s(T)/\gamma_{ST} T$ 曲线 vs 温度（数据表格或从图中提取的点）
- 低温 $C/T$ vs $T^2$ 的拟合斜率和截距
- 杂质/磁场下的 $C(T)$ 数据点
- 第二临界温度 $T_{c1}$ 的值

---

## 3. 图片与可视化材料

### 3.1 决策树

- 当前提取了 PDF 第 1 页的整页截图（PNG, 864KB）
- **问题**：低分辨率，中文 LaTeX 渲染可能不完整
- **需要**：
  - 原始矢量图（PDF/EPS/SVG）
  - 或由 workflow 作者重新绘制的高清版本
  - 确认图中的每个分支标签文字是否完整准确

### 3.2 论文中可能有的其他图

如果 ref6（或 cluster 中其他论文）包含以下图，请提供：

- $\Delta(T)$ 曲线
- $N(E)$ vs $E$（超导态与正常态对比）
- $C_s(T)/\gamma T$ vs $T/T_c$ 曲线
- 低温 $C/T$ vs $T^2$ 图
- 不同杂质浓度/磁场下的 $C(T)$

---

## 4. 多带/多分量序参量细节

workflow 结论提到了多分量序参量和两次跳变，但实现中完全没有这些内容。

| 信息 | 需要 |
|------|------|
| 哪些 paper 讨论了两带/两分量模型 | ref id |
| 两个序参量 $\Delta_1$、$\Delta_2$ 的关系 | 耦合？独立？ |
| $d_{x^2-y^2}+id_{xy}$ 的具体实现 | 哪个 phase？参数？ |
| 第二 $T_{c1}$ 的值 | 数值 |

---

## 5. 杂质与磁场参数

| 信息 | 当前状态 | 需要 |
|------|---------|------|
| $\gamma_0$ (杂质展宽) | 代码中有占位值 | 论文中使用的典型值 |
| $E_H$ (磁场能标) | 同上 | Zeeman 能标换算关系 |
| 杂质散射类型 | 未分类 | unitary / Born？ |
| 脏 d 波的 vortex 贡献 | ref11 提及 | 具体公式和参数 |

---

## 6. 具体软件/工具信息

当前 `tools` 数组为空，因为原文只说了"数值积分器（未报告具体软件）"。

| 信息 | 需要 |
|------|------|
| ref6 作者实际使用什么软件 | Fortran? MATLAB? |
| 17 篇 cluster 论文中哪篇报告了具体工具 | 可以填入 tools 数组 |
| 有没有可复用的公开代码仓库 | URL |

---

## 7. 数据集和实验对照

| 信息 | 需要 |
|------|------|
| 综述引用的比热实验数据来源 | 哪个实验组？哪个样品？ |
| 是否涉及公开数据库 | 如 Materials Project、COD 等 |
| 理论计算与哪个实验进行了定量对比 | 实验论文引用 |

---

## 总结：下一次迭代最需要的 3 件东西

1. **ref6 的 $f(\hat{k})$ 解析形式和费米面平均 $\langle f^2 \rangle$ 数值** — 没有这个就没法做各向异性计算
2. **一组完整的验证数据点**（$\Delta(0)$、$\gamma_{ST}$、$C(T)$ 曲线上几个 $T$ 的值）— 用于校准和确认管线正确
3. **决策树的高清矢量版本** — 用于 HTML 展示